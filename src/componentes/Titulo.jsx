/* Titulo */

const Titulo = ({label}) => {
  return (
    <div>
        <div className="titulo__productos">
            <h2 className="categorias">{label}</h2>
        </div>
    </div>
  )
}; export default Titulo
